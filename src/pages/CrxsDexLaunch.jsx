import CrxsDexLaunchPanel from "@/components/crxs/CrxsDexLaunchPanel";

// Admin page — owner-signed Aerodrome market launch control for CRXS.
// Admin-only: it signs with the CRXS launch wallet and writes the market record.
export default function CrxsDexLaunch() {
  return <CrxsDexLaunchPanel />;
}